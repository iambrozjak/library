<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Books</h1>
    <%= link_to "New book", new_book_path, class: "btn-blue" %>
  </div>

  <div id="books" class="grid grid-cols-3 gap-10">
    <%= render partial: "books/book", collection: @books %>
  </div>
</div>
