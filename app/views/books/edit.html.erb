<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing book</h1>

    <%= render  partial: "books/form", book: @books %>

  <%= link_to "Show this book", book_path(@book), class: "btn" %>
  <%= link_to "Back to books", book_path(@book), class: "btn" %>
</div>
